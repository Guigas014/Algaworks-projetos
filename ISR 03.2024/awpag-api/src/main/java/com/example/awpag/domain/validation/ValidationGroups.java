package com.example.awpag.domain.validation;

//Essa interface é usada para tratar o erro do clienteId quando é cadastrado um parcelamento.
// E é usado na entidade Cliente no atributo id.
public interface ValidationGroups {

      public interface ClienteId {
      }
}
